# Basic Notes on Classes in Javascript

## Basic Definition of a class
- `class NameOfClass { }`
- Define properties: `name; age;`
- Define constructor: `constructor(name, age) { set name and age using 'this' }`
- Define getters: `get name() { return this.name; }`